const alunos = ['Luiz', 'Maria', 'João'];

// alunos.unshift('Luiza') // Adiciona no começo
// alunos.unshift('Fabio') // Adiciona no fim
// alunos.pop(); Remove no fim
// alunos.shift(); Remove o primeiro
// delete alunos[1];
// console.log(alunos.slice(0, -2));

console.log(alunos);