const nome = 'Luiz';
const nome1 = "Luiz"; // string
const nome2 = `Luiz`; // string
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined nao aponta pra local nenhum na memoria ou seja nao fazer isso e sempre usar o null
let sobrenomeAluno = null; // Nulo -> não aponta pra local nenhum na memoria
const aprovado = true; // boolean = true, false (logico)

console.log (typeof nome, nome1);